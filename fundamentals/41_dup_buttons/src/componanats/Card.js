import React from "react";
import Button from "./Button";

const Card = () => {
  return (
    <Card className="card">
      <Button text="not important" />
      <Button text="important" />
    </Card>
  );
};

export default Card;
